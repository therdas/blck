# Build

blck doesn't need to be built. Just put it in a folder, run install.zsh and you're off
to the races.
This, however, is not an guarantee that blck will not need to be built in the future.

## Install

Some installs

### Express Install

Interactive Install
    sh -c "umask g-w,o-w;git clone https://github.com/therdas/blck.git $HOME/.blck;zsh $HOME/.blck/install.zsh"
Non-Interactive Install
    sh -c "umask g-w,o-w;git clone https://github.com/therdas/blck.git $HOME/.blck;zsh $HOME/.blck/install.zsh -q"